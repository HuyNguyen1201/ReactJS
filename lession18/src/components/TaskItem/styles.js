const styles = (theme) => ({
    card:{
        marginTop: 10
    },
    cardActions:{
        display:'flex',
        alignItems:'center',
        justifyContent: 'flex-end'
    }
});
export default styles;