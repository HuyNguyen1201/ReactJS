import {configure, shallow} from 'enzyme'
import NavigationItems from './NavigationItems'

describe('<NavigationItems/>',()=>{
    it('should render tow <NavigationItems> elements if not authenicate',()=>{
        const wrapper = shallow()
    })
});